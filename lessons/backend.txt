uv is a package manager much advanced than pip.it is written in rust and is much faster in installing dependenies and other.
it also doesnt require to manually activate a virtual environment. it does it automatically in the back!

but to use it we need to use the 'uv run' to start the app like 
'uv run python app.py'
to get the right environment


initializa command
uv init <name>

to add any package
uv add <package name>

uvicorn is a light weight high peformance web server used to run the python web applications
uvicorn is the messenger or the server..it reciesves the http requests and passes them  on to fastapi app and responds!


fastapi is the strucuture of the backend


create_engine from sqlalchemy creates the connection to your database
(how does my app talk to the database)

sessionmaker
this creates database sessions
(to open/close conversations with the database)


declarative_base is for defining models(tables)
so when you use this along with python classes then.. the sqlalchemy can understand that this is a table.


sqlite:///./app.db
sqlite is the database
///file path
./app.db file in the current folder

then we make the engine (which is the connection pool and the database manager)
check_same_thread==true indicates that to use the connection in one thread(not needed for fastapi (async))

use sessions to make the convo with database(high level)
the engine is low level



now instead of writing the schema in the function itslef at the parameters like email:str passwor:str and so on 
we write them as a object in the schemas.py so that they can be reusable and we can add more things neatly!
this object is the pydantic object
so whenever we write the object which is refer to the schema to a python class and the fastapi to recognize we write the baseModel



we do the db commn in sessions because without it the db would be inconsistent.by session we have like a transaction like things
so we can directly rollback or commit the way we want 
and not crash the program or db

schemas is not necessary but done to make it better writing


we are creating a db object whenever we call an api since we want it like short lived and we dont make any mistakes





